pre = '/local/users/pstar/cruise/data/ladcp/uh/pro/jc1802/ladcp/proc/matprof/h/';

fnamesd = dir([pre 'j*_02.mat']);

nz = 323; ns = length(fnamesd);

uv_dld = NaN+zeros(nz, ns); uv_dlu = uv_dld;
uv_uld = uv_dld; uv_ulu = uv_uld;
m = zeros(nz, ns); n = m;

for no = 1:length(fnamesd)

   stn = fnamesd(no).name(2:4);
   